import 'package:get/get.dart';

/// A base class for View Models that extends [GetxController].
///
/// This abstract class serves as a base for implementing View Models in the GetX framework.
/// It extends [GetxController], providing access to reactive state management capabilities.
abstract class ViewModel extends GetxController {
  ViewModel();
}
